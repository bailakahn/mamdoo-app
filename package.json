{
  "scripts": {
    "native": "cd native && yarn start -c",
    "provider": "cd provider && yarn start",
    "proxy": "cd proxy && yarn start",
    "start": "concurrently --kill-others  \"npm run provider\" \"npm run proxy\" \"npm run native\"",
    "deploy:provider": "heroku git:remote -a mamdoo-prvider && git subtree push --prefix provider heroku master",
    "deploy:proxy": "heroku git:remote -a mamdoo && git subtree push --prefix proxy heroku master"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^6.0.0"
  },
  "dependencies": {
    "history": "^5.0.0"
  }
}
